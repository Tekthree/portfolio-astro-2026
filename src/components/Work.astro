---
const projects = [
  {
    title: 'The Butterfly CFI',
    src: '/assets/images/69d55440de29525bb5ec52a8_the_butterfly_cfi_logo_mock_up.jpg',
    tag: 'Branding & Identity',
  },
  {
    title: 'Spencer Grey Art',
    src: '/assets/images/69d555693be7d4ffefb2a937_spencer_grey_website_on_pixel_jones_portfolio_site.jpg',
    tag: 'Web Design & Dev',
  },
  {
    title: 'DSC Family Reunion',
    src: '/assets/images/69d55e6c12ad0b79a8a3f9ae_dsc_family_reunion_pixel_jones.jpg',
    tag: 'Web Design',
  },
  {
    title: 'Tremulant',
    src: '/assets/images/69d55fe73960cc20e9554315_tremulant_poster_pixel_jones.jpg',
    tag: 'Print & Poster Design',
  },
  {
    title: 'Shameless Productions',
    src: '/assets/images/6881dc72a12b39532697f0cd_Screenshot_2025-07-24_000936.avif',
    tag: 'Web Design & Dev',
  },
  {
    title: 'Kobase Landing Page',
    src: '/assets/images/6881dcfee52d43ae2ba5a170_Screenshot_2025-07-24_001242.avif',
    tag: 'Web Design',
  },
  {
    title: 'House Cleaning Records',
    src: '/assets/images/6881dd39f62b061a6e246c06_Screenshot_2025-07-24_001343.avif',
    tag: 'Web Design & Dev',
  },
];
---

<section id="work" class="work-sec">
  <img
    src="/assets/images/62f6c3cffd7b99342f2f8896_right-art.avif"
    alt=""
    class="work-art-left"
    aria-hidden="true"
  />

  <div class="work-container">
    <div class="work-watermark" aria-hidden="true">
      <span class="work-watermark-text">MY WORK MY WORK MY WORK</span>
    </div>

    <p class="work-recent-label">Recent Work</p>
    <div class="nav-divider work-divider"></div>

    <div class="splide work-splide">
      <div class="splide__track">
        <ul class="splide__list">
          {projects.map(proj => (
            <li class="splide__slide work-card" data-trigger="hover">
              <img src={proj.src} alt={proj.title} class="work-card-img" loading="lazy" />
              <div class="work-card-overlay"></div>
              <div class="work-card-info">
                <p class="work-card-tag">{proj.tag}</p>
                <h5 class="work-card-title">{proj.title}</h5>
              </div>
            </li>
          ))}
        </ul>
      </div>
    </div>
  </div>
</section>

<style>
.work-sec {
  background-color: var(--swatch--dark-900);
  padding-top: var(--_spacing---section-space--small);
  padding-bottom: var(--_spacing---section-space--large);
  overflow: clip;
  position: relative;
}

.work-art-left {
  position: absolute;
  top: 43%;
  left: 0;
  /* fluid clamp, min 13.6rem @ 20rem viewport → max 31.7rem @ 90rem viewport */
  height: clamp(13.6rem, 8.43rem + 25.86vw, 31.7rem);
  z-index: 0;
  pointer-events: none;
}

.work-container {
  max-width: var(--max-width--main);
  margin: 0 auto;
  padding-left: var(--site--margin);
  padding-right: var(--site--margin);
  position: relative;
  z-index: 1;
}

.work-watermark {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  overflow: clip;
  pointer-events: none;
  z-index: 0;
}
.work-watermark-text {
  font-family: 'Mortend Outline', sans-serif;
  font-weight: 800;
  font-size: 11em;
  color: var(--swatch--light-100);
  opacity: 0.04;
  white-space: nowrap;
  text-transform: uppercase;
  letter-spacing: -0.02em;
  display: block;
}

.work-recent-label {
  opacity: 0.37;
  text-transform: uppercase;
  font-family: var(--_typography---font--primary-family);
  font-size: var(--_typography---font-size--text-main);
  font-weight: 700;
  color: var(--swatch--light-100);
  margin-top: var(--_spacing---space--8);
  margin-bottom: var(--_spacing---space--3);
  position: relative;
  z-index: 1;
}

.work-divider {
  position: relative;
  z-index: 1;
  margin-bottom: var(--_spacing---space--5);
}

/* Splide carousel — full width */
.work-splide {
  overflow: visible;
  position: relative;
  z-index: 1;
}
.work-splide .splide__track { overflow: visible; }
/* hide Splide arrows entirely */
.work-splide .splide__arrows { display: none; }

.work-card {
  position: relative;
  border-radius: var(--radius--main);
  overflow: clip;
  cursor: pointer;
}

.work-card-img {
  width: 100%;
  /* fluid clamp, min 13.4rem @ 20rem viewport → max 22.1rem @ 90rem viewport */
  height: clamp(13.4rem, 10.91rem + 12.43vw, 22.1rem);
  object-fit: cover;
  display: block;
  border-radius: var(--radius--main);
  transition: transform 0.6s cubic-bezier(.544,.375,.25,1);
  transform: scale(calc(1 + 0.08 * var(--_trigger---on, 0))) rotate(calc(2deg * var(--_trigger---on, 0)));
}

.work-card-overlay {
  position: absolute;
  inset: 0;
  background-image: linear-gradient(5deg, color-mix(in srgb, var(--swatch--dark-900) 92%, transparent), rgba(255,255,255,0) 55%);
  border-radius: var(--radius--main);
  pointer-events: none;
}

.work-card-info {
  position: absolute;
  inset: auto 4% 6% 4%;
  display: flex;
  flex-direction: column;
  gap: var(--_spacing---space--1);
}

.work-card-tag {
  font-family: var(--_typography---font--primary-family);
  font-size: var(--_typography---font-size--text-small);
  font-weight: 400;
  color: var(--swatch--light-100);
  opacity: 0.7;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin: 0;
}

.work-card-title {
  font-family: var(--_typography---font--heading-family);
  font-weight: 800;
  font-size: var(--_typography---font-size--h5);
  line-height: 1.1;
  color: var(--swatch--light-100);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  margin: 0;
}

/* Tablet */
@media screen and (max-width: 991px) {
  .work-art-left { top: 89%; }
}

/* Mobile — decorative art repositioning + carousel overflow only; sizes now fluid */
@media screen and (max-width: 767px) {
  .work-art-left { top: 94%; }
  .work-watermark-text { font-size: 5em; }
  .work-splide { overflow: clip !important; }
  .work-splide .splide__track { overflow: clip !important; }
}

/* Small mobile */
@media screen and (max-width: 479px) {
  .work-art-left { left: -7%; }
}
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const el = document.querySelector('.work-splide');
    if (!el || typeof Splide === 'undefined') return;
    new Splide(el, {
      type: 'loop',
      perMove: 1,
      perPage: 3,
      focus: 0,
      gap: '1.5em',
      arrows: false,
      pagination: false,
      speed: 600,
      updateOnMove: true,
      breakpoints: {
        991: { perPage: 2, gap: '1em' },
        767: { perPage: 1, gap: '1em' },
        479: { perPage: 1, gap: '1em' },
      },
    }).mount();
  });
</script>
